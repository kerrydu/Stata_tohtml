# 审稿相关修改总结

本文档汇总本轮针对审稿意见与易用性改进，在 `tohtml` / `ishere` 中已完成的修改（截至 2026-08-12）。

当前版本头：`tohtml.ado` **1.8**，`ishere.ado` **1.6**。

---

## 1. CSS 默认：包资源 `tohtml.css`（回应审稿 #43）

**审稿关切：** CSS 不应硬编码在 ado 里逐行拼接；应作为 F 类资源文件安装，便于维护。

**改动：**
- 新增包资源 `tohtml.css`（`tohtml.pkg` 中为 `F tohtml.css`）
- 指定 `html()` 且未指定 `css()` 时，经 `findfile` 复制默认 `tohtml.css` 到输出目录的 `css/`
- `css(githubstyle)` / `css(tohtml)` 仍可作为该默认文件的别名（兼容旧写法）
- 仍可用 `css("mystyle.css")` 覆盖为自定义样式
- 已删除 Mata 内嵌的 `write_github_css()`

**示例：**
```stata
tohtml "analysis.log", html("analysis.html") replace
* 使用包内 tohtml.css
```

---

## 2. MathJax 独立选项（回应审稿 #42）

**审稿关切：** MathJax 不应绑在 githubstyle 上；自定义 CSS 也应能开公式；无公式时不应链接大 JS。

**改动：**
- 新增独立选项 `mathjax`（与 `css()` 无关，可与自定义 CSS 合用）
- 仅在指定 `mathjax` **且** 在清理后的 Markdown 中检测到公式定界符（`$...$` / `$$...$$` / `\(...\)` / `\[...\]`）时才注入 CDN 脚本
- 未指定 `mathjax` 时绝不注入

---

## 3. SMCL 日志自动 translate（回应审稿 #45）

**审稿关切：** 帮助示例用 SMCL，忘记 `translate` 会报错；何不自动转换？

**改动：**
- `tohtml`（含 `clean` / `cleancode`）检测输入为 `.smcl`，或文件首个非空行为 `{smcl}` 时，自动 `translate ..., translator(smcl2log)` 到临时 text log，再继续处理
- 输出 `.md` / `.html` 的默认命名仍基于**原始**输入路径（或 `html()` / `md()`），不用临时文件名
- 帮助中的完整流程改为 text log（`log using ..., text`）；并说明 SMCL 可直接传入、无需手写 `translate`

**示例：**
```stata
tohtml "analysis.smcl", html("report.html") replace
* 等价于先 translate 再处理 text log
```

---

## 4. 选项 `cleanmd()` → `md()`，默认与 HTML 同名

**动机：** 回应审稿对 “clean” 命名的疑惑（#8），并让 Markdown / HTML 输出成对、易记。

**改动：**
- 选项由 `cleanmd(filename)` 改名为 `md(filename)`
- 未指定 `md()` 且指定了 `html()` 时：Markdown 路径 = HTML 路径，后缀改为 `.md`  
  （如 `html(report.html)` → `report.md`）
- 二者都未指定时：用输入文件词干 + `.md`
- `md()` / `html()` 缺后缀时分别自动补 `.md` / `.html`
- 不再使用 `*_clean.md` / `*.clean.md` / `*_code.md` 作为默认名

**示例：**
```stata
tohtml "analysis.log", html("report.html") replace
* → report.md + report.html
```

---

## 5. 自包含报告：`bundle` / `zip`（回应审稿 #44a）

**审稿关切：** HTML 不像 docx/pdf 那样内嵌图表；交给同事时需要连同 CSS、图、表一起传递。

**方案：** 在 HTML 输出端统一整理包目录，可选再打 zip。

**包结构（`ROOT` = `html()` 所在目录）：**
```text
ROOT/
├── report.html
├── report.md         （默认与 html 同名；若与 html 同目录，可打进 zip）
├── css/
├── figures/
└── tables/
```

**选项：**
| 选项 | 作用 |
|------|------|
| `bundle` | 收集本地 `<img>` / `<iframe>` 资源到 `figures/`、`tables/`，改写为相对路径 |
| `zip(.)` / `zip(name)` | 先 `bundle`，再用 Stata `zipfile` 打包；`.` 表示与 HTML 同名的 zip |

**其他相关：**
- `outreg2e` 表内 CSS 改为相对路径候选（`../css/`、`./css/`），便于 iframe 嵌入
- MathJax 仍走 CDN（离线公式需联网），帮助中已说明
- 多文件夹 / clean 目录模式：不必整目录复制；`bundle` 按引用收集文件即可

**示例：**
```stata
tohtml "analysis.log", html("report/report.html") zip(.) replace
```

---

## 6. 删除 `rpath` / `wpath`（回应审稿 #15）

**动机：** 与 `bundle`/`zip` 目标重叠，且增加学习成本；可移植分发已由 bundle 覆盖。

**改动：**
- 从 `tohtml` 及子程序语法中移除
- Mata 中 `rewrite_md` / `rewrite_md2` / `merge_cmdlog_blocks` 不再做路径字符串替换
- 更新 `tohtml.sthlp`、投稿稿示例、`doishere.do`

**替代用法：** 本地分享 / 上传网站 → 使用 `bundle` 或 `zip(.)`。

---

## 7. `html()` 自动补全 `.html` 后缀（回应审稿 #9 部分）

**改动：** 若 `html()` 参数不以 `.html` 结尾（大小写不敏感），自动追加 `.html`；不再因“其他扩展名”报错。

**示例：**
```stata
tohtml "a.log", html("report") replace      // → report.html（及默认 report.md）
tohtml "a.log", html("report.html") replace // 不变
```

（审稿还建议无参 `html` 默认 `logname.html`——尚未做。）

---

## 8. Markdown 叙事块：只认 `/**` … `**/`（回应审稿 #4）

**审稿关切：** 不必同时用 Stata 与 Markdown 两套块标记；可用类似 markdoc 的 `/**` / `**/`。

**最终规则：**
- **唯一**叙事块标记：`/**` … `**/`（可带 log 前缀 `.` / `>` / `{com}` 等）
- **已移除：** `ishere /*` … `ishere */`（若仍出现，`tohtml` 报错并提示改用 `/**` `**/`）
- 不成对 / 重叠时报错：`Error: unmatched /** and **/` 等

**示例：**
```stata
/**
Narrative markdown here.
**/
```

---

## 9. 重构 `cleancode`（回应审稿 #12）

**审稿关切：** log 已回显全部命令；再要求 `cleancode(dofile)` 易与实际跑 log 的 do-file 不一致。

**新逻辑（只分析 log）：**
- **保留：** 以 `.` 开头的命令行、以 `>` 开头的续行、`<img` / `<iframe` 嵌入
- **删除：** 不以 `.`/`>` 开头的命令输出

**语法变更：**
```stata
* 旧：tohtml "example1.log", cleancode("example1.do") html(...) replace
* 新：tohtml "example1.log", cleancode html(...) replace
```

**实现要点：** Mata `keep_stata_code_lines()` → `rewrite_md_cleancode()` → 复用现有 `rewrite_md` 流水线。

---

## 10. `clean` 与 `cleancode` 互斥（回应审稿 #13）

同时指定时报错并退出 `r(198)`：
```text
options clean and cleancode may not be combined
```

帮助中已说明二者不可合用。

---

## 11. 简化 `ishere` 命令

**目标：** 降低语法面，占位符与输出职责清晰分离。

**Mode 1 — 占位符（无输出）：**
| 语法 | 含义 |
|------|------|
| `ishere` | 代码块边界 |
| `ishere # ...` | Markdown 标题 |

**Mode 2 — 输出（emit）：**
| 语法 | 含义 |
|------|------|
| `ishere display ...` | 与 `display` 相同，写出 macro/scalar 等格式化值；供后续 `/**` 块内 `{ishere display ...}` 标签替换 |
| `ishere fig\|figure using filename [, zoom() height() width()]` | 写出 `<img>`（`using` 必选） |
| `ishere tab\|table using filename [, height() width()]` | 写出 `<iframe>` / md 占位（`using` 必选） |

**其余一律报错**，包括：`ishere /*`、`ishere */`、无 `using` 的 fig/tab、未知参数等。

叙事 Markdown 请用 do-file 注释 `/**` … `**/`（由 `tohtml` 识别，见第 8 节）。`tohtml` 将每个 `ishere display` 的输出替换进其后**第一个**文本块中的同名 `{ishere display ...}` 标签。

---

## 涉及的主要文件

| 文件 | 变更概要 |
|------|----------|
| `tohtml.ado` | `tohtml.css`/`mathjax`、SMCL 自动 translate、`md()` 命名、bundle/zip、去 rpath/wpath、`/**`、cleancode、互斥 |
| `tohtml.css` | 默认样式（包 F 资源） |
| `tohtml.pkg` | 增加 `F tohtml.css` |
| `tohtml.sthlp` | 选项、完整流程（text log）、SMCL 说明同步 |
| `ishere.ado` / `ishere.sthlp` | codeblock/header + `display` + fig/tab using |
| `outreg2e.ado` | 表 CSS 相对路径 |
| `doishere.do` 等 | `md()` 新语法 |

---

## 对审稿意见的对应关系（简表）

| 审稿点 | 我们的回应 | 状态 |
|--------|------------|------|
| #4 不必双重标记 Markdown / Stata 块 | 叙事块统一 `/**`…`**/`；去掉 `ishere /*` `*/` | 已做 |
| #8 “clean” 命名 / 默认 md 名 | `cleanmd`→`md`；默认与 `html` 同名 | 已做 |
| #9 `html` 自动补后缀；无参默认名 | 已自动补 `.html`；无参 `html` 默认尚未做 | 部分 |
| #10 默认 githubstyle CSS | 默认包资源 `tohtml.css` | 已做 |
| #12 `cleancode(dofile)` 不一致 | 只从 log 抽命令，不再要 do-file | 已做 |
| #13 `clean`/`cleancode` 未互斥 | 同时指定报错 | 已做 |
| #15 `rpath`/`wpath` 难懂 | 删除，改由 `bundle`/`zip` | 已做 |
| #42 MathJax 绑 CSS / 无公式也链接 | 独立 `mathjax`；有公式才注入 | 已做 |
| #43 CSS 硬编码在 ado 中 | 包资源 `tohtml.css`（F 文件） | 已做 |
| #44a 自包含 / zip | `bundle` / `zip` | 已做 |
| #45 SMCL 需手写 translate | 自动检测并 `smcl2log` | 已做 |
| #22/#23/#25 日志头与代码围栏 | — | 未做 |
| #28b `.md` iframe 内联 | 有逻辑，主路径有 bug 风险 | 未完全 |
| #36–38 依赖提示（勿静默装包） | pathutil 仍可能自动 `ssc install` | 未完全 |
| #44b base64 内联图 / 表嵌入 | — | 未做 |

---

## 建议的下一步（未做，仅备忘）

- 投稿正文 / 回复信中按上表写清“已改 / 如何用”
- 网站示例按新语法（`/**`、`md()`、`cleancode`、`zip`）重跑并更新
- 优先代码：#22/#23/#25 日志头与围栏；#28b；依赖检测提示；可选 #9 无参 `html`、#44b 内联
- 若需完全离线公式，可再评估本地 MathJax（体积较大，非本轮范围）
