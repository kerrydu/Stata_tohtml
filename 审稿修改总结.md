# 审稿相关修改总结

本文档汇总本轮针对审稿意见与易用性改进，在 `tohtml` / `ishere` 中已完成的修改（截至 2026-08-11）。

---

## 1. CSS 默认使用 GitHub 风格

**动机：** 减少每次生成 HTML 时手写 `css(githubstyle)` 的负担。

**改动：**
- 指定 `html()` 且未指定 `css()` 时，自动使用 `githubstyle`
- 仍可用 `css("mystyle.css")` 覆盖为自定义样式
- 已更新 `tohtml.sthlp`

**示例：**
```stata
tohtml "analysis.md", html("analysis.html") replace
* 等价于以前的 css(githubstyle)
```

---

## 2. 自包含报告：`bundle` / `zip`（回应审稿人关于 HTML 分发的意见）

**审稿关切：** HTML 不像 docx/pdf 那样内嵌图表；交给同事时需要连同 CSS、图、表一起传递。

**方案：** 在 HTML 输出端统一整理包目录，可选再打 zip。

**包结构（`ROOT` = `html()` 所在目录）：**
```text
ROOT/
├── report.html
├── report_clean.md   （若与 html 同目录，可打进 zip）
├── css/
├── figures/
└── tables/
```

**选项：**
| 选项 | 作用 |
|------|------|
| `bundle` | 收集本地 `<img>` / `<iframe>` 资源到 `figures/`、`tables/`，改写为相对路径 |
| `zip(.)` / `zip(name)` | 先 `bundle`，再用 Stata `zipfile` 打包；`.` 表示与 HTML 同名的 zip |

**其他相关：**
- `outreg2e` 表内 CSS 改为相对路径候选（`../css/`、`./css/`），便于 iframe 嵌入
- MathJax 仍走 CDN（离线公式需联网），帮助中已说明
- 多文件夹 / clean 目录模式：不必整目录复制；`bundle` 按引用收集文件即可

**示例：**
```stata
tohtml "analysis.log", html("report/report.html") zip(.) replace
```

---

## 3. 删除 `rpath` / `wpath`

**动机：** 与 `bundle`/`zip` 目标重叠，且增加学习成本；可移植分发已由 bundle 覆盖。

**改动：**
- 从 `tohtml` 及子程序语法中移除
- Mata 中 `rewrite_md` / `rewrite_md2` / `merge_cmdlog_blocks` 不再做路径字符串替换
- 更新 `tohtml.sthlp`、投稿稿示例、`doishere.do`

**替代用法：** 本地分享 / 上传网站 → 使用 `bundle` 或 `zip(.)`。

---

## 4. `html()` 自动补全 `.html` 后缀

**改动：** 若 `html()` 参数不以 `.html` 结尾（大小写不敏感），自动追加 `.html`；不再因“其他扩展名”报错。

**示例：**
```stata
tohtml "a.md", html("report") replace      // → report.html
tohtml "a.md", html("report.html") replace // 不变
```

---

## 5. Markdown 叙事块：只认 `/**` … `**/`（回应“不必双重标记”）

**审稿关切：** 不必同时用 Stata 与 Markdown 两套块标记；可用类似 markdoc 的 `/**` / `**/`。

**最终规则：**
- **唯一**叙事块标记：`/**` … `**/`（可带 log 前缀 `.` / `>` / `{com}` 等）
- **已移除：** `ishere /*` … `ishere */`（若仍出现，`tohtml` 报错并提示改用 `/**` `**/`）
- 不成对 / 重叠时报错：`Error: unmatched /** and **/` 等

**示例：**
```stata
/**
Narrative markdown here.
**/
```

---

## 6. 重构 `cleancode`（回应“不应再依赖原始 do-file”）

**审稿关切：** log 已回显全部命令；再要求 `cleancode(dofile)` 易与实际跑 log 的 do-file 不一致。

**新逻辑（只分析 log）：**
- **保留：** 以 `.` 开头的命令行、以 `>` 开头的续行、`<img` / `<iframe` 嵌入
- **删除：** 不以 `.`/`>` 开头的命令输出

**语法变更：**
```stata
* 旧：tohtml "example1.log", cleancode("example1.do") html(...) replace
* 新：tohtml "example1.log", cleancode html(...) replace
```

**实现要点：** Mata `keep_stata_code_lines()` → `rewrite_md_cleancode()` → 复用现有 `rewrite_md` 流水线。

---

## 7. `clean` 与 `cleancode` 互斥

同时指定时报错并退出 `r(198)`：
```text
options clean and cleancode may not be combined
```

帮助中已说明二者不可合用。

---

## 8. 简化 `ishere` 命令

**目标：** 降低语法面，占位符与输出职责清晰分离。

**Mode 1 — 占位符（无输出）：**
| 语法 | 含义 |
|------|------|
| `ishere` | 代码块边界 |
| `ishere # ...` | Markdown 标题 |

**Mode 2 — 输出（emit）：**
| 语法 | 含义 |
|------|------|
| `ishere display ...` | 与 `display` 相同，写出 macro/scalar 等格式化值；供后续 `/**` 块内 `{ishere display ...}` 标签替换 |
| `ishere fig\|figure using filename [, zoom() height() width()]` | 写出 `<img>`（`using` 必选） |
| `ishere tab\|table using filename [, height() width()]` | 写出 `<iframe>` / md 占位（`using` 必选） |

**其余一律报错**，包括：`ishere /*`、`ishere */`、无 `using` 的 fig/tab、未知参数等。

叙事 Markdown 请用 do-file 注释 `/**` … `**/`（由 `tohtml` 识别，见第 5 节）。`tohtml` 将每个 `ishere display` 的输出替换进其后**第一个**文本块中的同名 `{ishere display ...}` 标签。

---

## 涉及的主要文件

| 文件 | 变更概要 |
|------|----------|
| `tohtml.ado` | 默认 CSS、bundle/zip、去 rpath/wpath、html 后缀、仅 `/**`/`**/` 叙事块、cleancode 重构、互斥检查 |
| `tohtml.sthlp` | 上述选项与示例同步 |
| `ishere.ado` / `ishere.sthlp` | codeblock/header + `display` + fig/tab using；`using` 必选 |
| `outreg2e.ado` | 表 CSS 相对路径 |
| `stata_journal_submission.md` | 可移植性与 cleancode 表述更新 |
| `example_cleancode.do`、`docs/example3.do` 等 | 新 `cleancode` 语法 |

当前版本头：`tohtml.ado` **1.5**，`ishere.ado` **1.6**。

---

## 对审稿意见的对应关系（简表）

| 审稿点 | 我们的回应 |
|--------|------------|
| HTML 不能自包含图/表/CSS，不便交给同事 | `bundle` / `zip` + 规范目录 |
| `rpath`/`wpath` 类路径方案偏绕 | 删除，改由 bundle 承担可移植性 |
| 不必双重标记 Markdown / Stata 块 | 叙事块统一为 `/**` … `**/`；去掉 `ishere /*` `*/` |
| `cleancode(dofile)` 可能与真实 log 不一致 | 改为只从 log 抽命令，不再要 do-file |
| 标记体系偏复杂 | `ishere` 保留 codeblock/header、`display`、fig/tab；叙事块统一 `/**`/`**/` |

---

## 建议的下一步（未做，仅备忘）

- 投稿正文 / 回复信中按上表写清“已改 / 如何用”
- 网站示例（如 example1–3）去掉 `ishere /*`/`display`，按新语法与 `cleancode`/`zip` 重跑并更新
- 若需完全离线公式，可再评估本地 MathJax（体积较大，非本轮范围）
